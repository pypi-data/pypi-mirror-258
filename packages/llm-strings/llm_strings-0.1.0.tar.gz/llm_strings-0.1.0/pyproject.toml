[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "llm_strings"  # This should match the name in setup.py
version = "0.1.0"
description = "A brief description of my package"
authors = [{name = "Juan Wisznia", email = "juanwisznia@gmail.com"}]
license = {file = "LICENSE"}
readme = "README.md"
