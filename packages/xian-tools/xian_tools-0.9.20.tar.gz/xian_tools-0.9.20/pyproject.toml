[tool.poetry]
name = "xian-tools"
version = "0.9.20"
description = "Core utils to interact with Xian network"
authors = ["endogen"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
pynacl = "^1.5.0"
requests = "^2.31.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
