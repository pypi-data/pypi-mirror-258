[tool.poetry]
name = "freshgui"
version = "0.1"
description = "A module for creating python GUI's. Lightweight, simple, begginer-friendly."
authors = ["watakak"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
