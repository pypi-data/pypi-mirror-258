[build-system]
requires = [
    "setuptools",
    "wheel",
]

[project]
name = "ouroboros_hf_text_generation"
version = "1.0.1"
description = "Simple hugging face text generation utility package with multiple prompt format support and history."
readme = "README.md"
authors = [{name= "VINUK"}]
license = { file = "LICENSE" }

 
dependencies = [ "torch>=2.1.1", "transformers>=4.37.1", "accelerate>=0.26.1","optimum>=1.16.2", "rich"]
 
requires-python = ">=3.10"
