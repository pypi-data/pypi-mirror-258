[project]
name = "fractus"
dependencies = ["cffi"]

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "fractos.fractos"
python-source = "python"
strip = true