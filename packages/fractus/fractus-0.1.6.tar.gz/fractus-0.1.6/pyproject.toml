[project]
name = "fractus"
dependencies = ["cffi>=1.16.0"]

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
features = ["python"]
module-name = "fractus.fractus"
python-source = "python"
strip = true