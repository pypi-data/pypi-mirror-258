import { Parser } from './base';
import { IDefinition } from './formatTypes';
import { MSet } from '../setMethods';
/**
 * Parser for our custom JSON schema format.
 */
export declare class JsonParser extends Parser {
    start(): Promise<void>;
    processDefinition(data: IDefinition): void;
    readonly widgetNames: MSet<string>;
    protected _names: MSet<string>;
}
