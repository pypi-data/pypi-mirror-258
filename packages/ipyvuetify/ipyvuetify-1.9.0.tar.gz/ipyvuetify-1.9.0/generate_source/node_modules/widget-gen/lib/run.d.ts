export declare function run(filename: string, languages: string[], options: Partial<IOptions>): Promise<void | void[]>;
export interface IOptions {
    output: string;
    parserName: string;
    templateFile: string;
    fileExt: string;
}
