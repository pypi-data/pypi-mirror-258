[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
dependencies = ["torch"]
name = "simp-hint"
version = "0.0.2"
readme = 'README.md'
requires-python = '>= 3.10'

[project.urls]
"Homepage"= "https://gitlab.com/TJ-coding/super-simple-pytorch-type-hint"