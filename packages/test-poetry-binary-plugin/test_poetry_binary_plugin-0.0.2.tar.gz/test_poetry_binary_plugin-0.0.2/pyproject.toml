[tool.poetry]
name = "test-poetry-binary-plugin"
version = "0.0.2"
description = ""
authors = ["Thomas Mahé <contact@tmahe.dev>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
rich = {version = "^13.7.0", optional = true}

[tool.poetry.extras]
with-deps = ["rich"]

[tool.poetry.scripts]
test-input = "test_poetry_binary_plugin.main:test"
#test2 = {"type" = "file", "reference" = "dist/pyinstaller/single-file-bundled"}
#test3 = {"type" = "file", "reference" = "dist/pyinstaller/folder-bundled/folder-bundled"}

[tool.poetry-pyinstaller-plugin.scripts]
#hello-world = "test_poetry_binary_plugin/main.py"
#single-file = { source = "test_poetry_binary_plugin/main.py", type = "onefile"}
single-file-bundled = { source = "test_poetry_binary_plugin/main.py", type = "onefile", bundle = true}
folder-bundled = { source = "test_poetry_binary_plugin/main.py", type = "onedir", bundle = true}

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
